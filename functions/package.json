{
  "name": "functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@genkit-ai/firebase-functions": "^1.20.0",
    "@genkit-ai/google-genai": "^1.20.0",
    "dotenv": "^16.5.0",
    "firebase-functions": "^5.0.1",
    "genkit": "^1.20.0",
    "genkitx-firebase": "^1.20.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5",
    "genkit-cli": "^1.20.0"
  }
}
